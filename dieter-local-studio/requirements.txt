# Local DIETER Studio — no cloud APIs. CPU/GPU friendly.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
numpy>=1.26.0
scipy>=1.11.0
soundfile>=0.12.0
librosa>=0.10.0
# Optional: pip install madmom (may need extra build deps on Windows)
# madmom>=0.16.1
