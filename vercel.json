{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci && cd mureka-clone && npm ci",
  "buildCommand": "cd mureka-clone && npm run build && cd .. && rm -rf public && mkdir -p public && cp -r mureka-clone/dist/. public/",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [{ "source": "/((?!api(?:$|/)).*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
