# Web API (pinned friendly to Railway/Render; bump as needed)
fastapi>=0.104.1,<0.116
httpx>=0.25.0,<1
uvicorn[standard]>=0.24.0
gunicorn>=21.0
pydantic>=2.4,<3
python-multipart>=0.0.6

# Optional: production metadata / auth templates
supabase>=2.4.0
# python-jose[cryptography]  # enable when implementing HTTPBearer auth

# Hugging Face (MusicGen / optional stacks)
transformers>=4.33.0,<5

# Numerics / audio analysis
numpy
librosa>=0.10.0
soundfile
scipy
audioread

# FFmpeg bindings (optional helpers; production mixing also uses `ffmpeg` binary on PATH)
ffmpeg-python

# --- Pure Song: Coqui TTS + torch (large; CPU OK on MacBook Air / Windows) ---
# If install fails, use CPU wheels: https://pytorch.org/get-started/locally/
TTS==0.22.0
torch>=2.0,<3
torchaudio>=2.0,<3

# Optional: stronger beat / downbeat models (heavy install)
# madmom

# --- MusicGen (Audiocraft) — large deps & ~10GB+ weights; optional ---
# Enable server-side: DIETER_ENABLE_MUSICGEN=1
# Install may conflict with other torch stacks; use a dedicated venv if needed.
audiocraft>=1.3.0
