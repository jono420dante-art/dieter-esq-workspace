services:
  - type: web
    name: dieter-studio-api
    env: node
    buildCommand: npm install
    startCommand: npm run start
    plan: free
    envVars:
      - key: PORT
        value: 3001
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
